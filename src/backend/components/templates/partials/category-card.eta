<a class="category-card" href="/category/<%= it?.name?.toLocaleLowerCase() %>" title="See assets in this category">
  <div class="container">
    <h3><%= it?.name %></h3>
    <span><%= it?.count %> assets</span>
  </div>
</a>
