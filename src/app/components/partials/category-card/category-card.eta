<a class="category-card" href="<%= it?._locals?.functions?.buildCategoryPath(it?.name) %>" title="See assets in this category">
  <div class="container">
    <h3><%= it?.name %></h3>
    <span><%= it?.count %> assets</span>
  </div>
</a>
